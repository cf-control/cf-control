apiVersion: 1.0.0
target:
  endpoint: 'api.pcfsys.dev.datev.de'
  org: LOON
  space: Master-Secure
spec:
  spaceDevelopers: ['t01234@datev.de', "other.mail@mailserbce.de"]
  services:
    mitarbeiter-web-route:
      service: datev-http-routing
      plan: secure
      tags:
      - tag1
      - tag2
    service-database:
      service: elephantsql
      tags:
        - tag1
  apps:
    firstApp:
      path: src/app-1.0.1.jar
      manifest:
        buildpack: someBuildpack2
    secondApp:
      path: src/app2-1.0.jar
      manifest:
        buildpack: other buildpack
        services:
        - some service